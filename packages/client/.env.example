# WebSocket URL of the Colyseus server.
# Defaults to ws://localhost:2567 when unset (local dev).
# For production builds point it at your deployed server, e.g.:
# VITE_SERVER_URL=wss://bomberman-server.fly.dev
VITE_SERVER_URL=ws://localhost:2567
